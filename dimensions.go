package tilemap8x8x256x256

const (
	MapWidth   = 256 // MapWidth  is the width  of the map in the number of tiles (not pixels).
	MapHeight  = 256 // MapHeight is the height of the map in the number of tiles (not pixels).
	TileWidth  = 8 // TileWidth  is the width  of a tile (in pixels).
	TileHeight = 8 // TileHeight is the height of a tile (in pixels).
	Depth      = 1 // the size of a uint8
)
